<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">
	<select id="findByEmail" resultType="user">
		select email,password from L3_USERS where email = #{email}
	</select>

	<insert id="create">
		insert into L3_USERS(email, password) values (#{email},#{password})
	</insert>

	<delete id="deleteUserByEmail">
		delete from L3_USERS where email = #{email}
	</delete>
	
<!-- 	<update id="update">
		update USERS 
		set password= #{password}, name =#{name}, email = #{email} where userId= #{userId} 
	</update> -->
</mapper>